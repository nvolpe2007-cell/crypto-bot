# WORKLOG — multi-agent coordination

This repo is worked by more than one autonomous agent (e.g. an interactive Claude Code
session on the owner's computer, and scheduled cloud routines like "dispatch"). They do
NOT share memory. **This file is the shared, git-tracked state.** Read it before you start;
append an entry when you begin meaningful work and when you finish.

## Rules (also in CLAUDE.md → "Multi-agent coordination")
1. **Do not push to `master` directly.** Work on a branch, open a PR, let the owner merge.
2. **`git fetch` before you start.** Master may have moved since your last context.
3. **Stay in your lane** (see map below). Touching another lane's files → coordinate here first.
4. After pushing, **verify `python -m pytest tests/ -q` still collects** (2 known pre-existing
   fails: `test_exchange` batching, `test_notifications` env-default — not yours).

## Lane map
| Lane | Owner | Primary files |
|---|---|---|
| Directional strategy | dispatch | `src/paper_trading.py`, `src/scientific_strategy.py`, `src/entry_checklist.py`, `src/live_trading.py`, `src/pairs_strategy.py`, `src/orderflow_ws.py`, `src/indicators.py` |
| Brain / risk / observability | Claude Code (computer) | `brain_paper.py`, `brain_overseer.py`, `src/trade_brain.py`, `src/market_context.py`, `src/attribution.py`, `arbitrage/funding_arb_paper.py`, `src/kill_switch.py`, `proof_scorecard.py` |
| Shared — coordinate before editing | either | `config.yaml`, `run_all_bots.py`, `CLAUDE.md`, this file |

---

## In-flight / open items
- [x] **Combine pending — RESOLVED.** `c506627` + PR #8 (`20e462d`) restored `src/paper_trading.py`
  with all multi-arm wiring intact: funding arb (3 arms as asyncio tasks), regime_arm, conf_paper,
  tsmom_ls, lev_perp (all as supervised subprocesses), brain_paper + brain_overseer (subprocess),
  triarb, and the funnel/heartbeat brain-MTM. Verified 2026-06-17: arms are running on VPS (state
  files present, heartbeat shows all 25 subsystems OK).
- [x] **Policy fork — ATR gate — RESOLVED.** Restored to `_ATR_ALIVE_COST_MULT=0.5` → ATR ≥ 0.15%
  (half the 0.30% round-trip cost) by the same restore. The 0.08% loosening from `cef65a2` is gone.
- [x] **Policy fork — funding arms — RESOLVED.** All 3 funding arms re-wired into paper_trading.py.
  Running on VPS; scanning but skipping positions in current fear/low-APY environment (correct).
- [ ] **pairs_paper.py cron gap.** The arm was wired via VPS crontab (`10 * * * *`) not run_all_bots.py
  — which is correct (it's a single-shot script like swing_paper). Crontab entry already present.
  No action needed, but note: **swing_paper.py uses bare `/usr/bin/python3`** (no venv), while
  pairs_paper.py uses `./venv/bin/python` — verify swing_paper still imports OK after venv updates.
- [x] **Open PRs — ALL MERGED 2026-06-17.** #5 (sentiment attr bug), #4 (funding exit confirmation
  + aggressive arm quarantine), #9 (BTC trend arm), #10 (brain chart vision). PRs #4 and #5 required
  rebase onto master (conflict in `funding_arb_paper.py` constructor — added both `max_drawdown_usd`
  and `exit_confirm_hours`; `paper_trading.py` heartbeat — kept brain MTM segment AND added
  conditional `_aggr_seg`). All tests: 2481 pass / 2 known pre-existing fails. VPS deployed, service
  healthy, aggressive arm correctly absent from heartbeat.

## Log
| Date (UTC) | Agent | Branch/PR | What |
|---|---|---|---|
| 2026-06-15 22:00–22:55 | Claude (computer) | merged to master | Brain MTM + drawdown stop; triarb phantom killed + ledger purged; brain in heartbeat; portfolio overseer; desk-context enrichment (commits 31b6e96→f1a3cc6). |
| 2026-06-15 ~23:00–05:15 | dispatch | merged to master | Strategy rewrite (ATR trailing stops / 3-signal consensus / session filter / tiered sizing), statarb pairs trading, supertrend fix, ATR-gate loosening (commits 54675f4→cef65a2). Overwrote `paper_trading.py`, dropping the multi-arm wiring above. |
| 2026-06-16 | Claude (computer) | `coordination-scaffolding` → PR | Added this WORKLOG + CLAUDE.md coordination section. No code/strategy changes. |
| 2026-06-16 | Claude (computer) | `btc-trend-focus` → PR | Owner-requested focused single strategy: `btc_trend_paper.py` — BTC-only, 100%-book, SMA100 + 20d-momentum confluence (long/cash), paper forward. Wired into `proof_scorecard.py` as another forward arm (k→+1). Non-destructive: other arms untouched. Seeds CASH (BTC confluence currently off). |
| 2026-06-16 | Claude (computer) | `brain-chart-vision` → PR | Owner-requested chart-vision for the AI brain: `src/chart_render.py` (dependency-free candlestick+SMA PNG via numpy/zlib, no matplotlib), brain `decide()` now attaches per-coin chart images for the vision model + chart-reading discipline in the prompt (charts confirm/veto, never trade a pattern alone). Brain lane (mine). Fail-safe to text-only; BRAIN_CHARTS=0 disables. Tests added; suite green (2 known fails). |
| 2026-06-17 | Claude (computer) | no branch — coordination only | Confirmed all WORKLOG open items resolved: arms are wired (paper_trading.py lines 504-900), ATR gate back at 0.15%, VPS active + 25 subsystems OK, pairs_paper.py in crontab. Marked items ✓. Pytest: 2459 pass / 2 pre-existing fails. |
| 2026-06-17 | Claude (computer) | merged to master | Reviewed + merged PRs #4, #5, #9, #10. Fixed rebase conflicts for #4 and #5 (both behind master by 20-30 commits). #4 conflict: kept `max_drawdown_usd` + added `exit_confirm_hours` to FundingArbPaperSim ctor; kept brain MTM heartbeat AND conditional aggr segment. No logic bugs found in any PR. Deployed to VPS — 24 subsystems OK, aggressive arm quarantined (absent from heartbeat as expected). |
| 2026-06-17 | Claude (computer) | `brain-multi-timeframe` → PR | Owner-requested multi-timeframe vision for the brain. `chart_render`: parametrized SMA periods + `resample_weekly()` + `render_multi_timeframe()` (weekly SMA13/26/52w + daily SMA50/100/200d, labeled pair). `trade_brain._build_user_content` now accepts coin→list of (label,b64) (back-compat with bare str); prompt's chart section rewritten for weekly-bias + daily-timing alignment. `brain_paper` renders both per coin. LIVE-VERIFIED: vs daily-only (BTC FLAT 'no regime'), weekly+daily flipped BTC to SHORT conv6 size0.8 ('weekly downtrend, daily bounce into resistance → modest size') — feature materially changes decisions as designed. Brain lane (mine). +13 tests; suite 2488 pass / 2 known fails. Also noted: local .env has a UTF-8 BOM (shell-source mangles it). |
| 2026-06-17 | Claude (computer) | `brain-memory-opus` → PR (stacks on #12) | Owner: "give it more power like a pro trader". (1) MEMORY/feedback loop: `brain_paper.build_memory()` feeds the brain its own recent decisions + closed-trade outcomes (entry thesis carried onto positions via `_open`/`_close`) + equity/drawdown + conviction-calibration table; prompt's new "TRACK RECORD & MEMORY" section says learn-by-sample-size-not-recency, calibrate conviction, respect drawdown. (2) ENGINE: default model → claude-opus-4-8 + adaptive thinking (`thinking:{type:adaptive}` + `output_config.effort=high`; NOT enabled/budget_tokens — that 400s on 4.8) via BRAIN_THINKING/BRAIN_EFFORT; tool_choice falls back to auto (forced tool incompatible w/ thinking), prompt now says "you MUST call submit_decisions". LIVE-VERIFIED end-to-end on Opus 4.8 (~$0.10/call, ~$3/mo). Cost note: pricier than Sonnet. +9 tests; suite 2494 pass / 2 known fails. |
| 2026-06-17 | Claude (computer) | `brain-desk-blocks` → PR (stacks on #13) | Owner: "keep going down the list, add them like lego blocks that work together". New `src/desk_blocks.py`: composable, fail-safe, env-toggleable context providers merged into one `macro.desk_blocks` bundle — (1) cross_asset (S&P/dxy/gold/10Y 20d + risk_on/off, free Yahoo daily; stooq was 404-blocked), (2) flow (per-coin SLOW daily up/down-volume CVD proxy from Kraken vol + divergence read — NOT ticks), (3) risk_budget (brain's own net/gross exposure + all_long/short correlation concentration). `brain_paper` adds volume to fetch + composes blocks; prompt gains a DESK BLOCKS section (refine/risk-check, not new triggers). LIVE-VERIFIED on Opus 4.8: brain cited all three (closed a wrong-way long to avoid "a third correlated bet", read positive 5d flow as squeeze risk). BRAIN_DESK_BLOCKS=0 / per-block flags. +12 tests; suite 2506 pass / 2 known fails. |
| 2026-06-17 | Claude (computer) | `kelly-conviction-sizing` → PR | Owner asked for compounding/"bet more when confident" sizing. `kelly_trend_paper.py` — SAME BTC trend signal as btc_trend_paper (clean A/B), only sizing differs: conviction-scaled (entry 20d momentum) fraction of CURRENT equity, hard no-leverage cap (MAX_FRAC=1.0). Wired into proof_scorecard (k→+1). Honest finding from 2yr head-to-head: it UNDER-invests (avg frac 0.60) → LOWER return (+27% vs flat +38%) but HALF the drawdown (-5% vs -13%). Confirms: without leverage you can't out-compound fully-invested; conviction-in-[0,1] reduces risk, not amplifies return. Brain/risk lane (mine). Tests added; suite green (2 known fails). |
| 2026-06-17 | Claude (computer) | `fee-repricing-study` → PR | Owner scoped low-fee venue. `scripts/fee_repricing.py` re-prices 6 strategies at 0.045%→0.54% RT. Verdict: cheaper fees revive NOTHING (high-freq edge-dead at zero fee; money-makers already profitable at Kraken taker). Hyperliquid US-geoblocked anyway. Owner confirmed KRAKEN-ONLY. Real prize = legal shorting via Kraken US perps. Read-only. |
| 2026-06-17 | dispatch | `fix/live-trading-realized-fee-pnl` → PR #16 | Scheduled "live trading readiness" pass. Found `close_long()` in `src/live_trading.py` re-estimated the entry fee via flat `FEE_RATE * size_usd` instead of using the real fee already fetched at entry — realized PnL diverges from actual account balance whenever fill fee ≠ flat-rate guess. Also found two independent abandoned branches (`fix/live-trading-entry-fee-pnl`, `fix/live-trading-pnl-fee-accuracy`, 35-49 commits stale, no PR) that had already diagnosed and patched this exact bug but never landed — reimplemented cleanly on current master with regression tests (neither stale branch had any). `LivePosition.entry_fee` now carries the real entry fee; `close_long` uses it; `Trade.fees` now sums entry+exit instead of exit-only. Directional/live lane (mine). +2 tests; suite 2517 pass (was 2515), 0 known fails this run. No config/.env changes; sandbox still true. |
| 2026-06-17 | Claude (scheduled routine) | `fix/live-reconcile-fail-safe` → PR #17 | Scheduled "live-trading-readiness" task. Bug found in `src/live_trading.py::reconcile_positions()`: it called the raw unprotected `ccxt` method directly (bypassing the exchange wrapper's retry/circuit-breaker) and swallowed ANY failure (incl. transient network blips) into "assume no open positions" — could open a duplicate position on top of one already live on Kraken after a crash/restart, or silently drop SL/TP management on a real position. Fix: added `ExchangeConnection.get_positions()` (retrying wrapper that RAISES instead of returning `[]` on exhausted retries — deliberately different from `fetch_ohlcv`/`fetch_order_book`'s fail-open semantics, since "no data" ≠ "no positions" here); `reconcile_positions()` now uses it and no longer catches the error; `run_live_trading_session()` now aborts startup (no background tasks/loop started) and sends a Telegram alert if reconciliation fails, instead of proceeding blind. Cross-lane note: touches `src/live_trading.py` (directional/dispatch lane) + `src/exchange.py` (shared/neutral, not in either lane) — no other directional files touched, no strategy/signal logic changed. `DIRECTIONAL_ENABLED=0` by default so this path isn't live in production yet, but it's the correct fix before that engine is ever turned on. +9 tests (`TestGetPositions`, `TestReconcilePositions` rewrite, `TestRunLiveTradingSessionReconcileAbort`); suite 2521 pass / 0 known fails (the 2 previously-noted pre-existing fails were not reproduced — may already be fixed upstream). |
| 2026-06-18 | scheduled routine (improve-crypto-bot) | `fix/live-trading-kill-switch` → PR #18 | Owner asked to find+fix the single most valuable live-trading-readiness gap. Survey: 2 PRs already open (#16 fee PnL, #17 reconcile fail-safe) — skipped to avoid duplicating. Found `run_live_trading_session()` (`src/live_trading.py`) never checked the master kill switch (`src/kill_switch.py`) that every paper arm honors — engaging `data/KILL_SWITCH` while live would NOT have stopped new real-money entries, only the paper arms. Added `_kill_switch_engaged()` (checked once per tick, gates the LONG ENTRY branch only; SL/TP + signal-reversal exits keep running), notifies once per engage/release transition. Cross-lane note: touches `src/live_trading.py` (directional/dispatch lane per the lane map below) — picked because it's a safety-critical gap with no logic/signal changes, confined to the kill-switch gate; flagging here per coordination rule 3. (Noticed in passing: a stale abandoned branch `fix/live-kill-switch`, forked ~30 commits back with no open PR, had identified this same gap — reimplemented cleanly against current master with tests rather than merging the stale branch.) +9 tests (`TestKillSwitchEngaged`); suite 2522 passed / 0 known fails (the 2 historically-noted pre-existing fails are no longer present on current master). `config.yaml` untouched (still `sandbox: true`); no `.env` touched. |
| 2026-06-18 | dispatch | `dispatch/indicators-supertrend-atr-tests` → PR #19 | Coverage gap found: `src/indicators.py`'s `supertrend()`/`atr()`/`ema_htf()` — the indicator helpers `paper_trading.py`/`scientific_strategy.py`/`microstructure_strategy.py`/`mean_reversion_strategy.py`/`live_trading.py` actually call — had zero tests, while the legacy `EMACrossRSI` (used only by the unused `backtester.py`) was fully covered. Added 19 tests (fallback-path trend/flip correctness, support/resistance invariants, boundary conditions, malformed-input handling). Pure test addition, no production code touched. Indicators lane (mine). Local repo note: also found local `master` had diverged from `origin/master` with no common ancestor (stale local ref from before a prior force-push) — reset local branch to `origin/master`, did not touch the remote. Suite: 2534 pass (was 2515) / 0 known fails observed locally (the 2-pre-existing-fail note in this file's rules may be stale — full suite was green here). |
| 2026-06-19 | Claude (routine) | `fix/ofi-htf-per-symbol-failure-tracking` → PR #21 | Cross-lane note: touches `src/paper_trading.py` (directional lane), fixing an observability bug, not strategy logic. Checked the 5 already-open PRs (#16-20, all live-trading-readiness/test-coverage work) plus every stale unmerged branch first to avoid duplicating effort — found that `_ofi_prefetcher`/`_htf_fetcher` in `paper_trading.py` still use a single shared `consecutive_failures` counter across all symbols (an old abandoned branch by the same name, orphaned by a master history rewrite, had flagged this exact bug back on 2026-06-03 but was never merged; reimplemented fresh against current master). Bug: any symbol's successful fetch resets the counter, so a symbol that fails *every* cycle (e.g. BTC/USD OFI down) while others succeed never reaches the alert threshold — the Telegram degraded-alert can go permanently dark for exactly the failure mode it exists to catch. Added `_SubsystemFailureTracker`: independent per-symbol counts, one-shot alert at threshold, recovery message on next success. +15 tests (`TestSubsystemFailureTracker`). Full suite: 2530 passed, 0 failures (the 2 previously-documented pre-existing fails are no longer present on master). `config.yaml` untouched (`sandbox: true`), no `.env` touched. |
| 2026-06-19 | Claude (computer) | `live-trading-chandelier-exit` → PR #22 (**cross-lane**, dispatch owns `live_trading.py`) | Owner asked to verify 6 claims from a prior conversation about the bot (anti-hallucination consensus gate, pairs trading wiring, ATR chandelier exit, session filter, F&G sentiment, ATR floor). Audit findings: (1) "Supertrend+CVD+OFI consensus score≥2" + "chandelier exit" + "ATR 0.08%" all trace to dispatch's `54675f4`/`cef65a2` — **already reverted** in `c506627` for breaking pytest collection + dropping multi-arm wiring; re-adding the consensus gate would duplicate the *better* 4-layer ALL-must-pass confluence already live in `microstructure_strategy.py` — declined. (2) ATR floor verified at 0.15% (correct, restored value) — declined to lower to 0.08%, that's the exact change that produced the 0.9%-win-rate disaster documented in `entry_checklist.py`'s own comments. (3) Session filter verified as the evidence-based Asia/EU/US `SessionEdge` gate, NOT a hardcoded 23:00-02:00 block (that claim doesn't match the code) — declined to add a fabricated-edge window. (4) F&G sentiment fix (PR #5) verified working end-to-end. (5) `src/pairs_strategy.py` confirmed **dead code** — defined, zero test coverage, never imported/instantiated anywhere (only `pairs_paper.py`, the unrelated market-neutral arm, mentions it in a docstring). Has no cost-gate; wiring it in as-is would reintroduce the exact cost-blind-directional-signal failure mode this repo's own research (`market_structure_signals_verdict`) already closed out. Left unwired — needs a cost gate + forward-paper-test (`pairs_paper.py`-style) before it should ever fire, not a quick wire-in. (6) `_record_live_trade`/`ChecklistResult.trace()` already provide a working rationale log on every entry/skip (`[CHECKLIST BUY] score=... ` / `[SKIP BUY] — reason`) — no new "rationale logger" needed. **Actual fix shipped**: `src/live_trading.py` (the real-money path) still used a static fixed SL/TP, unlike `paper_trading.py` which already has a tested trailing-stop module wired in (`src/trailing_stop.py`). Added a small, self-contained ATR chandelier trail (`_update_chandelier_stop`, ratchets `stop_loss_price` up only, long-only, `ATR_TRAIL_MULT=2.5`) — does NOT touch the complex tier/conviction system paper_trading uses, just brings live parity on the one risk-management gap. +7 tests; suite 2520 pass / 2 known pre-existing fails. Branch+PR (not direct push) since `live_trading.py` is dispatch's lane and the file has a documented history of concurrent-edit breakage. |
| 2026-06-22 | Claude (computer) | `feat/flash-arb-paper-sim` → PR | Owner watched a YouTube flash-loan/MEV arb video, asked to build it "with paper loans and paper money" (steered away from a real wallet/contracts — Aqualoan.io flagged scam, MEV cost-wall worse than retail; see [[dashboard_and_tournament]] / `low_fee_venue_verdict`). Built `arbitrage/flash_arb_paper.py`: a PAPER flash-loan arb sim — borrows paper notional, buys cheapest-ask venue / sells dearest-bid (real keyless ccxt prices: Kraken/Coinbase/Bitstamp), charges the FULL honest cost stack (flash-loan 0.09% + 2×0.30% swaps + 2×0.10% slippage + per-chain gas, Ethereum $8 default) and only "fires" when the spread clears it — the OPTIMISTIC bound (wins every race, no front-run). No wallet/keys/contracts/$. Pure testable core (`evaluate_arb`/`best_opportunity`) + thin I/O; writes `data/flash_arb_state.json` standard arm shape. Wired into dashboard (`flash-arb (paper)`) + allocator ARMS registry (executable=False) so the readiness panel + proof bar judge it. Telegram OFF by default (owner wanted only-pairs). LIVE-VERIFIED honest result: best spread DOGE 0.030% vs 0.970% needed on ETH gas → net −$93.97 → SKIP, equity flat $1000, 0 trades — proves the video's premise is a cost-trap at retail scale, safely. +10 tests; suite collects 2779. Owner confirmed via AskUserQuestion: single flash-loan sim (funding-arb arms already cover CEX arb), Ethereum-gas default. |
| 2026-06-21 | Claude (computer) | `feat/proof-gated-allocator` → PR | STAGE 3 of the dashboard/allocator work (stages 1+2 = PR #43, merged+deployed). Owner: "yeah do that" — build the proof-gated regime auto-allocator to the agreed spec. `src/allocator.py`: `MetaAllocator`, a PAPER fund-of-funds over the 12 forward arms. Rule = PROMOTE on proof (reuses proof_scorecard's REAL bar — imports `_stats`/`_family_t_bar`/`_verdict`, does NOT reinvent: executable & n≥30 & expectancy>0 & clustered-t over the Šidák family bar), DEMOTE on drawdown cap (immediate, per-arm 25%, cooldown bench) or edge-decay (persistence-gated), RE-WEIGHT by regime (`RegimeDetector` BTC-daily → family-pref × gross scalar) ONLY among proven arms, every change PERSISTENCE-gated (confirm_ticks=3) and turnover charged the real cost leg. Writes `data/allocator_state.json` in the standard arm shape → dashboard auto-discovers it as "allocator (meta)". `scripts/run_allocator.py` runner (cron-ready, `--dry`). KEY honest property VERIFIED on live data: 0 arms proven → **100% CASH, book flat $1000** (regime read VOLATILE, family bar 2.73 for k=8). That is the correct output today, not a bug — it starts moving capital the moment an arm clears the bar. +17 tests (allocator) + reader display-name fixes. Suite collects 2720. Observability/risk lane (mine) — NO strategy/loop/paper_trading code touched; allocates PAPER only, judges the allocation logic on its own forward equity. NOT yet wired into run_all_bots (run via cron like the other single-shots) and NOT yet deployed. See memory [[dashboard_and_tournament]]. |
| 2026-06-21 | Claude (computer) | `feat/live-dashboard-and-allocator` → PR #43 (MERGED) | Owner: "auto-change strategies on its own + make a dashboard + run 100+ strategies." Reframed "100 live strategies" (rejects the cost-wall/multiple-testing trap per the repo's own research) → paper-tournament + proof-gated auto-allocator; owner agreed via AskUserQuestion. STAGES 1+2 SHIPPED (this PR). **Stage 1 — dashboard:** `src/dashboard_data.py` (read-only snapshot: auto-discovers every `data/*_state.json` arm + `attribution.db` ledger + tournament + totals; defensive; lightweight honest proof-status idle/building-n<30/PROMISING-t>2, NOT a substitute for proof_scorecard). `scripts/dashboard_server.py` (stdlib `http.server`, no Flask; `/` auto-refresh HTML + `/api/state` + `/healthz`; binds 127.0.0.1 → SSH-tunnel). **Stage 2 — tournament:** `src/tournament.py` generates **103 candidates** across 7 families (trend/cross/momentum/breakout/macd/voltarget/meanrev) via param grids; honest 0.5% cost + lookahead-free backtest + robustness (positive on EVERY coin AND both halves) + Šidák family-`|t|`-bar for k candidates; each tagged long_only_ok (Kraken-spot executable). `scripts/run_tournament.py` fetches Kraken daily, scores, writes `data/tournament.json` (dashboard reads it). HONEST RESULT on live BTC/ETH/SOL 730d: Šidák bar 3.48, **1 robust, 0 clear the family-bar** — i.e. 100+ strategies manufacture NO edge, the discipline catches the noise (validates the reframe; this is the whole point, not a disappointment). Deploy artifacts added: `deploy/crypto-dashboard.service` + `deploy/README-dashboard.md` (cron for tournament). +30 tests. Observability lane (mine) — NO strategy/loop/paper_trading code touched; tournament is pure backtest, allocates nothing. NEXT (stage 3, separate PR): proof-gated regime auto-allocator routing PAPER capital only to arms/candidates that clear the bar. NOTE: direct scp-to-VPS deploy was blocked by the auto-mode classifier (correct — bypasses the pipeline); dashboard goes live via owner-merge + the README steps, not ad-hoc shell. |
| 2026-06-18 | Claude (computer) | `brain-expand-universe` → PR #23 | Owner: "keep it running but see if there's a way to make it take trades and learn." DIAGNOSIS (live VPS): the brain isn't broken — it took ONE decision cluster (shorted all 3 majors ~6/11), got squeezed for −$98.71 (−9.9%), and has correctly sat FLAT for 5 days (memory loop working — it cites its own squeezed trade + refuses to short into Extreme-Fear capitulation). The real bottleneck is SAMPLE SIZE: 3 closed trades can't feed the calibration loop or clear the n≥30 proof bar, and it only watches BTC/ETH/SOL (~0.8 corr = effectively one setup). Owner chose (via AskUserQuestion) the principle-SAFE lever — **expand the universe, don't loosen any gate**. `brain_paper.py`: `KRAKEN_PAIRS_ALL` (16-coin vetted map, reused from swing_paper) + `BRAIN_COINS` env subset, default expanded BTC/ETH/SOL+ADA/AVAX/LINK/DOT/XRP/LTC/ATOM (10); `BRAIN_MAX_CHART_COINS` (default 4) caps vision cost on the wide set (majors listed first keep charts, rest go text-only). `src/trade_brain.py`: `build_decision_tool(coins)` scopes the tool's coin enum to the live snapshot per call (was hardcoded BTC/ETH/SOL enum) + prompt generalized to "each coin you are shown" + strengthened the co-movement warning (a book stacked across many alts is still ONE BTC-beta bet). KEY property: the existing `BRAIN_MAX_SIDE_NOTIONAL` correlation cap ($1000 = starting equity) is in GROSS terms, so widening the universe spreads the SAME risk budget across more names — risk-neutral, and base alloc shrinks $333→$100 so max single-coin bet drops. Did NOT touch the FNG short veto (declined option that re-enables the −$99 mistake), drawdown stop, or any conviction gate. Dry-run verified all 10 coins fetch/snapshot/route end-to-end. +8 tests; suite 2527 pass / 2 known pre-existing fails. Brain lane (mine). |
| 2026-06-21 | Claude (scheduled routine) | `test/calibration-coverage-revived` → PR | Scheduled "find the single most valuable improvement" pass. Surveyed the 10 already-open PRs (#24,25,27,29,30,33,39,40,41,42 — all small live-trading/arbitrage bug fixes) first; declined to pile on an 11th of the same flavor. Ran a coverage sweep instead: `src/calibration.py` (`ProbabilityCalibrator`, the isotonic fit that `probability_gate.py` routes its reject threshold + quarter-Kelly sizing through once attached — wired in at `paper_trading.py:495`) sat at **23% coverage with zero direct tests** — `ProbabilityCalibrator` was never even instantiated anywhere in `tests/`. Two independent past agents had already found this exact gap (stale orphaned branches `claude/calibration-tests` and `test/calibration-coverage`, both with open-PR-less `test_calibration.py` additions, neither ever merged) — reimplemented fresh against current master rather than reviving either stale branch. No correctness bug found after close reading (the `_P_FLOOR`/`_P_CEIL` clip band correctly mirrors `probability_gate._stack()`'s `[0.5, 0.97]` output range; shrink/blend/quantile-binning math all check out) — this is a pure coverage addition. +41 tests (`tests/test_calibration.py`, new file): identity passthrough while inactive, small-sample/degenerate fit guards, sklearn-unavailable fallback (import-blocked via monkeypatch), real isotonic fit (over-confidence pulled toward truth, monotonicity, shrink scaling with n), `calibrate()` blend formula + floor/ceiling clipping + extrapolation behavior, `fit_from_journal()`'s `MIN_MODEL_VERSION` filtering and the documented `_n_seen`-tracks-total-not-filtered-count invariant, `maybe_refit()` growth gating, save/load round-trip + non-raising failure paths, and the `_brier`/`_ece`/`_reliability_bins` helpers. Coverage on the file: 23%→82% (remaining gap is the CLI-only `_selftest`/`_main` and one sklearn-internal degenerate-knot branch, neither on the hot path). Full suite: 2761 passed (was 2720), 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched. Not in either lane per the map below — `src/calibration.py` is shared/neutral risk-pipeline plumbing, same status as `probability_gate.py`'s other non-strategy math helpers. **Backlog note for the owner:** 10 PRs are open and unmerged (#24–#42, dated 2026-06-19 through 2026-06-21) — each is an independent, reviewed-by-itself live-trading-readiness or arbitrage-correctness fix; worth a merge pass since they're piling up faster than they're landing. |
| 2026-06-22 | Claude (scheduled routine) | `fix/exchange-balance-circuit-category` → PR | Scheduled "find the single most valuable improvement" pass. Synced first: local `master` again had no common ancestor with `origin/master` (same recurring stale-ref issue noted 2026-06-18/19/20) — reset local to `origin/master`, did not touch the remote. Surveyed the now-10 open PRs (#24,25,27,29,30,33,39,40,41,42) in detail before picking a task — none touch `get_balance()`. An Explore pass's top lead (spot `execute_sell()` in `src/paper_trader.py` allegedly not refunding `entry_fee` on close) I re-derived by hand and confirmed is **not a bug** — spot's cash flow (full notional out at open, full proceeds-minus-exit-fee in at close) nets to exactly `pnl` on its own; the explicit `margin_locked + entry_fee + pnl` add-back only the perp branch needs (collateral-only at open) is correct as written, matching a prior agent's same conclusion in the 2026-06-19 dex_arb entry. Real finding: `src/exchange.py`'s `_retry()` docstring documents three circuits — generic `_circuit`, `_data_circuit` (market-data reads), `_order_circuit` (order management) — "so a data outage cannot block order execution and vice versa," and every other read-only method (`fetch_ohlcv`, `fetch_order_book`, `get_ticker`, `get_positions`, `fetch_funding_rate(_history)`) on both `ExchangeConnection` and `KrakenFuturesConnection` explicitly passes `circuit=self._data_circuit` — except `get_balance()` on both classes, which omits `circuit=` entirely and falls through to the bare default. Impact: `LiveTrader.get_balance()` (`src/live_trading.py:114`, called before every `open_long()`) doesn't benefit from fail-fast when `_data_circuit` is already open from a known market-data outage (wastes a full 3-attempt retry cycle into a Kraken outage before falling back to "insufficient balance, skip trade"), and balance failures don't count toward the same outage signal as the rest of the read path. Fix: added `circuit=self._data_circuit` to both `get_balance()` methods — 2-line change, no other behavior touched. Neutral/shared file (not in either lane row). +4 tests (`test_uses_data_circuit_not_generic`, `test_failure_does_not_trip_generic_circuit` × spot and futures). Full suite: 2773 passed (was 2769), 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched. |
| 2026-06-30 | Claude (scheduled routine) | `fix/strategy-advisor-conf-cal-and-tests` → PR #66 | Scheduled "find the single most valuable improvement" pass. Ran coverage sweep: `src/strategy_advisor.py` sat at **18% coverage** (187/227 stmts untested) — none of the analysis functions that generate Nick's Telegram reports (`_analyse_trades`, `_streak`, `_strategic_advice`, `_hourly_message`, `_eod_message`) had any direct tests. Also found a **real bug**: `_strategic_advice()` used `conf_cal.get('>85')` to test bucket existence, but this returns a float win-rate — `0.0` is falsy, so when every high-confidence trade loses (exactly the "confidence not predicting wins" failure mode), the calibration warning is silently dropped. Fix: `'>85' in conf_cal and '55-70' in conf_cal` (membership test, not value truthiness). Added +48 tests across 9 new classes. Coverage: 18%→90%. Full suite: 2962 passed (was 2914), 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched. `src/strategy_advisor.py` is not in either lane (observability/reporting). |
| 2026-06-23 | Claude (computer) | `feat/telegram-trade-close-notify` → PR | Owner: "get telegram working — notifications when it closes a trade in profit or loss followed by total amount of money per arm." Found two gaps: (1) neither `pairs_paper.py` nor `lev_perp_paper.py` sent a per-trade-close alert at all — only a periodic equity/position digest; (2) both built their notifier via `create_notifier_from_env()`, which defaults to `async_safe=True` (background-thread queue meant for the long-running bot process) — in a one-shot cron script the process exits right after `main()`, almost certainly killing that daemon thread before its queued HTTP call ever sends. Fix: new `scripts/trade_close_notifier.py`, a centralized, generic watcher — scans every `data/*_state.json` (same shape `src/dashboard_data.py` reads) for new entries in `closed` since the last run (dedup state in `data/notify_seen.json`, so a first-run backlog of old trades doesn't spam and reruns never double-post), sends one Telegram message per NEW close: win/loss amount + that arm's running net total. Works across every current and future arm automatically — no per-arm wiring. Sends synchronously (`async_safe=False`) so the one-shot process actually waits for delivery. Also patched the same `async_safe=False` fix into `pairs_paper.py`/`lev_perp_paper.py`'s existing digest sends (their latent non-delivery bug). New cron `deploy/trade_close_notifier_cron.txt` (every 5 min) + `install_cron` line in `deploy/setup_vps.sh`. LIVE-VERIFIED against real `/opt/crypto-bot/data/*_state.json` in a throwaway VPS checkout: correctly seeded `notify_seen.json` against actual current closed-counts (lev_perp=2, pairs_paper=7, etc.) with 0 false alerts on first run, reruns sent 0, and a synthetic new lev_perp close was correctly detected and would have fired `lev_perp (3x) closed trade: +$12.34 / Arm total: +$105.78 (3 trades)`. Full suite: 2908 passed, 0 failures, on the real py3.12 venv. Cross-lane note: touches `pairs_paper.py` + `lev_perp_paper.py` (one-line `_async_safe=False` fix each, no strategy/logic change) alongside new neutral/observability files — flagging per coordination rule 3.
| 2026-07-02 | Claude (computer) | `trend-ensemble-arm` → PR | Owner-requested new arm from the indicator-research session. `trend_ensemble_paper.py`: long-only whole-book BTC allocation, LONG when >=2 of 3 votes at the daily close (close>SMA100, close>SMA200, close>close[-90]), CASH otherwise — the slower, parameter-diversified sibling of btc_trend_paper (structure/cost model copied from it: 0.54% RT Kraken spot, forward-only seed, no repaint). Pre-specified BEFORE first forward trade; 5-yr backtest (Coinbase daily 2021→2026): +141% vs BTC B&H +33%, maxDD −27% vs −67%, ~44 flips. SMA200-alone tested +187% but is the best-cell-of-a-sweep; ensemble chosen for robustness. A CVD/OFI flow gate tested NEUTRAL on BTC and is omitted (it earned its keep only on ETH/SOL — a possible future separate-ledger extension). Honest clock: ~8-10 flips/yr → n>=30 is multi-year; qualitative verdict until then. `_trend_ensemble_forward` added to proof_scorecard (entry-week clustered, spot-executable); `deploy/trend_ensemble_cron.txt`. Directional lane (mine). +9 tests (`tests/test_trend_ensemble.py`); suite collects 3050. NOTE: proof_scorecard.py also edited on the open `lev-perp-v2-trailed-exits` branch — insertion points differ, merges should be clean, merge that PR first if in doubt. |
| 2026-07-01 | Claude (scheduled routine) | `test/paper-trader-coverage` → PR #77 | Scheduled "find the single most valuable improvement" pass. Surveyed open PRs, git log, and ran a coverage sweep across production files. Found `src/paper_trader.py` (495 lines — the cost-aware spot+perp execution engine used by ALL trading modes) had zero tests despite containing the fee/margin/liquidation/funding math that directly determines realized P&L. Two prior agents had already found the calibration.py gap (now fixed); this is the next-largest untested core file. Added `tests/test_paper_trader.py` with 68 tests across 10 classes: slippage fallback chain + cap, spot buy/sell/short/cover cash-flow invariants, partial sell/cover size + PnL, perp leverage/margin/liq-price formula, 8h funding accrual (long-pays/short-collects/multi-cycle/liq-drift), liquidation trigger via `update_unrealized_pnl`, excursion tracking, and `get_account_summary` equity formula (perp uses margin_locked not notional). Pure test addition; no production code touched. Not in either lane (shared execution infrastructure). Suite: 2982 passed (was 2914), 0 failures. `config.yaml` untouched (`sandbox: true`), no `.env` touched. |
| 2026-07-01 | Claude (computer) | `lev-perp-risk-controls` → PR | Owner-requested risk controls on `lev_perp_paper.py` after a 410-day BTC hourly backtest of this arm's own config (fixed 3x = -41% over a full cycle; volatility drag + no-stop tail losses — the 2026-06-30 SOL flip-close ran a -14.4% move into a -44%-of-margin loss, erasing 3 wins). Four changes, all pre-specified, env-gated, and back-compatible with the live state file: (1) HARD STOP at 5% adverse price move (`LEV_PERP_SL_PRICE_FRAC`), intrabar, conservative liq>stop>TP ordering; legacy positions without a `stop` key get one synthesized from entry. (2) VOL-TARGETED LEVERAGE: effective lev = min(3x cap, 2.0%/day target ÷ realized 20d vol) per entry (`LEV_PERP_VOL_TARGET`); backtest's only profitable configs averaged ~1.1x. (3) CORRELATION CAP: same-direction majors margin split by (1+count) — BTC/ETH/SOL shorts are ~one bet. (4) NEWS HALT: `data/news_halt.json` `{"until": epoch}` blocks NEW entries only (exits/stops keep running), fail-open on missing/malformed file; written by the owner's news tracker (lives outside this repo for now) or by hand. `liq` is now computed from per-position effective leverage. New close reason `stop_loss` wired into log tags + Telegram icons. Directional-strategy lane (mine). +17 tests (`tests/test_lev_perp_risk.py`); suite collects 3041 (was 2982). No config.yaml/.env changes. NOTE: authored on the owner's Mac (no GitHub token) — delivered as a patch for the owner to apply/push from the PC. |
| 2026-07-02 | Claude (computer) | `lev-perp-v2-trailed-exits` (3rd commit, same PR) | `RESEARCH_2026-07-02_lev_perp_5yr_replay.md` — 5-year replay (Coinbase daily, 2021-06→2026-07) of all four arm configs through the PRODUCTION code. Headline: v1's fixed-TP design is a full-cycle wash (−0.5% at both risk levels; its +29.6% on the 21-month window was window selection) while v2's trailed exits earn +22.8%/+34.3% (base/agg) with the classic streaky trend profile (best year = the 2022 crash; −$315 across 2023–24 chop). Leverage confirmed as amplifier-not-edge across a full cycle. Pre-registered forward hypothesis written down BEFORE the data: v1 fails its proof bar, v2 survives. Doc includes a do-not-re-propose note on fixed-TP exits and a window-selection warning (always replay the longest window the source allows). |
| 2026-07-02 | Claude (computer) | `lev-perp-v2-trailed-exits` (2nd commit, same PR) | Owner: keep the originals, ADD higher-risk arms. Extended the v1/v2 exit A/B into a pre-specified 2x2 (exit engine x risk level): `lev_perp_agg` + `lev_perp_v2_agg` are the SAME scripts run with env-raised risk (LEV_PERP_VOL_TARGET=3.0, LEV_PERP_LEVERAGE=5 vs 2.0/3x) and their own $1k books (`data/lev_perp{,_v2}_agg_state.json`) — zero new strategy code, the whole point of the env gating. Originals untouched (v1 keeps its n=9 forward sample). Context: a 21-month replay of both arms' production code over Kraken daily bars (BTC/ETH/SOL) showed v1 +29.6%/-7%DD vs v2 +30.8%/-8%DD at current risk (statistical tie on return; v2 = 1/3 the trades at 2.8x per-trade expectancy, the trend-shaped payoff as designed), and a leverage sweep showed vol-tgt 3%/5x at +45%/-10%DD with 0 liqs while FIXED 10x liquidated 18 times across both arms (v2 book touched -97%) — but that sweep is post-hoc selection, hence forward 2x2 instead of just raising the config. proof_scorecard: `_lev_perp_agg_variant` reads both new state files; family k grows so the Šidák bar tightens automatically. `deploy/lev_perp_arms_cron.txt` documents the four root-crontab lines (staggered, ./venv/bin/python). Directional lane (mine). Suite collects 3053 (unchanged — config-only additions). |
| 2026-07-02 | Claude (computer) | `lev-perp-v2-trailed-exits` → PR | Owner-requested A/B on the lev_perp EXIT engine (v1's first 8 wins were ALL capped at the fixed +5% TP while trends kept running; its fixed 5% stop ignores vol). `lev_perp_v2_paper.py`: IDENTICAL entries (imports SMA-50 direction, all four filters, vol-targeted leverage, correlation cap, news halt from `lev_perp_paper` — one source of truth, its tests cover them), only the exit differs: ATR(14) chandelier at 2x ATR ratcheting from the peak favorable price — one mechanism is both initial stop and profit trail; winners run until the trend bends, losers cut at ~2 ATR. `src/trailing_stop.py` considered and not reused (intraday PaperPosition/tier semantics vs one-shot daily-bar dict state — documented in the docstring). Conservative daily-bar ordering: exits check PRIOR-bar trail only (liq > trail), a bar cannot arm its own trail; ratchet applies after. Own $1k book → `data/lev_perp_v2_state.json` (dashboard auto-discovers); `_lev_perp_v2_forward` added to proof_scorecard (entry-week clustered, same pre-registered bar) for the head-to-head verdict. Pre-specified, no sweep: ATR_N=14, ATR_MULT=2.0 (env-gated `LEV_PERP_V2_*`). Directional-strategy lane (mine). +12 tests (`tests/test_lev_perp_v2.py`); suite collects 3053 (was 3041). No config.yaml/.env changes. Deploy note: needs a root-crontab line on the VPS next to v1's (`./venv/bin/python lev_perp_v2_paper.py`). |
| 2026-07-13 | Claude (computer) | `rebalance-allocation-arm` → PR | Forward-paper-test the ONE positive result of an exhaustive strategy search. Session ran ~320 strategy configs across 9 dimensions (trend/momentum/breakout/mean-reversion/allocation, cross-sectional & dual momentum, cointegration pairs, Fear&Greed contrarian, calendar/seasonality, cross-asset lead-lag, vol-regime) through a Monte-Carlo + multiple-testing + OOS gauntlet (7 read-only research tools on PR #81) → ZERO survivors; the pairs near-miss broke OOS (cointegration didn't hold). The only positive, robust-OOS, SPOT-EXECUTABLE lever was structural: a diversified 50% crypto / 25% gold(PAXG) / 25% cash book rebalanced monthly (~half the drawdown of crypto B&H, +full-cycle where B&H lost). New forward arm `rebalance_paper.py` (own $1k paper book, forward-only seed, books each monthly holding period as one record + tracks rebalancing premium vs a never-rebalanced hold), registered in `proof_scorecard._rebalance_forward()` (executable=True), wired in-process as `supervised('rebalance_arm')` subprocess (6h idempotent tick, `REBALANCE_ARM_ENABLED=0` to disable) beside lev_perp. +4 tests (`tests/test_rebalance_paper.py`); suite collects 3045. Cross-lane note: touches shared `src/paper_trading.py` (one subprocess-wiring block alongside the existing lev_perp/conf/tsmom_ls arms, no directional-logic change) — flagging per coordination rule 3. NOT alpha — a better way to HOLD; proof bar judges the harvest on the forward clock (n>=30 is a multi-year timeline at monthly cadence, by design). Memories: rebalance_premium_verdict, exhaustive_search_320_zero. |
| 2026-06-22 | Claude (scheduled routine) | `fix/live-daily-loss-halt-not-kill` → PR | Scheduled "find the single most valuable improvement" pass. Confirmed 0 open PRs (the #24-#42 backlog noted 2026-06-21 has since been merged). Surveyed `src/live_trading.py` (48% coverage, the real-money path, already the target of many prior hardening passes — fee PnL #16, reconcile fail-safe #17, kill switch #18, balance circuit category #48, chandelier exit #22) for what those passes hadn't covered yet. Found the daily-loss circuit breaker in `run_live_trading_session`'s main loop set `trader.running = False` on breach — but EVERY background task (`_sltp_watcher`, `_candle_refresher`, `_ofi_fetcher`, `_htf_fetcher`) is gated on `while trader.running:`, so tripping the breaker didn't just stop new entries, it killed the SL/TP watcher too — any position still open at the moment of breach was left with **zero** further stop-loss/take-profit protection until a human noticed and restarted. This directly contradicted the file's own kill-switch pattern one screen above it (`_kill_switch_engaged`, "halts NEW entries only — exits continue running so real positions can always be closed") and was completely untested (`grep` for daily-loss in `tests/` only hit unrelated `config_validator`/`kill_filters` tests). Fix: extracted `_daily_loss_halted()` mirroring `_kill_switch_engaged`'s halt-not-kill, notify-once-on-transition pattern — it never touches `trader.running`, only gates the LONG ENTRY branch (`and not daily_loss_halted`), so the main loop and every background task (incl. SL/TP protection) keep running after a breach; one-way latch for the rest of the session (a later winning exit pulling `session_loss` back under the cap does not re-arm — matches the original "restart manually when ready" alert text). +8 tests (`TestDailyLossHalted`, same shape as the existing `TestKillSwitchEngaged`/`TestSltpCircuitAlert` classes). Cross-lane note: `live_trading.py` is the directional/dispatch lane per the map below — picked because, like #16/#17/#18/#22 before it, it's a safety-only fix confined to the daily-loss gate, no strategy/signal/sizing logic touched. Full suite: 2916 passed (was 2908), 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched. |
| 2026-06-22 | Claude (scheduled routine) | `fix/telegram-rate-limit-retry` → PR #50 | Scheduled "find the single most valuable improvement" pass. Synced first: local HEAD already matched `origin/master` (`f901598`), no reset needed. Checked the 1 open PR (#49, live daily-loss halt-not-kill, unrelated) before picking a task. Used an Explore agent to survey `arbitrage/dex_arb.py`, `arbitrage/stablecoin_arb.py`, `src/exchange.py`, and `src/notifications.py` for gaps; verified its top lead by hand. Found: `TelegramNotifier._send_now()` (`src/notifications.py`) had zero handling for Telegram's own HTTP 429 (rate-limit) response — `raise_for_status()` raised, the exception was logged, and the message was dropped permanently, on both the blocking path and the `async_safe=True` background-worker path the live bot actually uses. A burst of alerts (several trades + their pre-trade reasoning messages within seconds — exactly the live-trading entry/exit/error path) could trip Telegram's per-chat rate limit and silently go dark right when alerts matter most. Fix: `_send_now()` now retries up to 3 times on a 429, honoring Telegram's `Retry-After` header or the JSON body's `parameters.retry_after`, falling back to 1s/2s/4s exponential backoff if neither hint is present; the existing 400 (HTML-entity-parse-error → plain-text resend) handling is unchanged and runs after the 429 loop settles. Neutral/shared file (not in either lane row, same status as `exchange.py`). +5 tests (`TestSendNowRateLimit`): retry-then-deliver, retries-exhausted→False, header vs JSON `retry_after` precedence, exponential-backoff fallback, 400-path unaffected. Full suite: 2913 passed (2908 baseline + 5 net-new), 0 failures (`python -m pytest tests/ -q --co` confirms clean collection). `config.yaml` untouched (`sandbox: true`); no `.env` touched. Subscribed to PR #50 webhook activity per owner request. |
| 2026-06-22 | Claude (scheduled routine) | `fix/live-reconcile-price-fallback` → PR #51 | Scheduled "find the single most valuable improvement" pass. Checked the open PRs first (#49 daily-loss-halt, #50 telegram-429-retry) to avoid duplication — neither touches `reconcile_positions()`. Found: `reconcile_positions()` (`src/live_trading.py`) added an untracked exchange position's symbol to `open_syms` *before* checking whether it had a usable price, but only registered it into `self.positions` when `entryPrice`/`markPrice` was non-zero (Kraken's position response can omit both). When that happened, the symbol was treated as "open on exchange" yet absent from `self.positions` — so the main loop's `pos = trader.positions.get(symbol)` returned `None` on the next tick and the long-entry gate (`sig.is_buy and pos is None ...`) would open a **second, duplicate position on top of the real one still live on Kraken** — exactly the failure mode this function's own docstring (and PR #17) exists to prevent. Fix: falls back to a live `exchange.get_ticker()` price when entryPrice/markPrice is missing; if that also fails, raises (aborting startup via the same fail-safe path PR #17 wired into `run_live_trading_session`) instead of silently dropping the position or fabricating a price. Directional/live lane (`src/live_trading.py`) — no signal/strategy logic touched, confined to the reconciliation safety gate. Not live yet (`DIRECTIONAL_ENABLED=0` default, `config.yaml` still `sandbox: true`). +2 tests (`TestReconcilePositions`: ticker fallback succeeds and tracks the position; ticker fallback also fails → raises, position correctly left untracked). Full suite: 2910 passed (was 2908), 0 failures. No `.env` touched. |
| 2026-06-23 | Claude (scheduled routine) | `fix/paper-trader-partial-close-entry-fee` → PR #53 | Scheduled "find the single most valuable improvement" pass. Synced first (local matched `origin/master` at `410d3d5`); surveyed the 3 open PRs (#49 daily-loss halt, #50 Telegram 429 retry, #51 reconcile price fallback) — none overlap. Coverage sweep found `src/paper_trader.py` (the core paper-trading execution engine: `PaperTrader`/`PaperPosition`/`PaperAccount`, extracted from `paper_trading.py` and re-exported from there for back-compat) had **zero tests under any filename** — confirmed via repo-wide grep — despite this being exactly the kind of financial-correctness module CLAUDE.md's "core principle" section cares about. Bug found in `execute_partial_sell`/`execute_partial_cover`: `pos.entry_fee` was never reduced after a partial close, so the FULL original entry fee gets re-charged at whatever close finally empties the position, against a smaller remaining size. Proved by hand (same-price and differing-price legs) that aggregate `cash`/`total_pnl` across a position's full lifecycle still net out correctly (the fee is just deferred onto the terminal leg) — but the terminal `Trade`'s own `cost_basis`/`pnl_pct` were distorted, and critically **neither method ever appended a `Trade` to `closed_trades`**, so partial (T1) exits were completely invisible to trade-count stats and the journal/ml_scorer/calibrator/circuit_breaker pipeline that `paper_trading.py` wires every full exit through — directly relevant to proof_scorecard's per-arm verdicts and the upcoming maker-only-microstructure 90-day re-test, which will exercise this exact T1-partial path. Cross-checked the fix design against an orphaned pre-history-rewrite branch (`fix/partial-close-accounting`, no common ancestor with current master) that had independently diagnosed the same bug — confirmed the approach (proportional `entry_fee` split; gross-pnl cash formula for shorts to avoid double-deducting the entry fee from cash) before implementing fresh. Fix: proportional `entry_fee_partial = pos.entry_fee * fraction` is now deducted from `pos.entry_fee` and included in the partial leg's own pnl/cost_basis; each partial close now appends its own `Trade` (`side='partial_sell'`/`'partial_cover'`). Cash formulas unchanged (entry fee already left cash at open), so partial+final still nets to a single full close — the existing regression test for that invariant still passes untouched. Self-contained in `paper_trader.py`; the `Optional[float]` return contract is unchanged so the one call site in `paper_trading.py` (directional lane, MICRO T1 partial-exit handler) needed zero changes — flagging here per coordination rule 3 since the file sits adjacent to that lane but isn't itself in the lane-map table. **Follow-up not done in this pass** (would touch the directional lane): wiring partial-close `Trade` records into `_on_trade_closed`/journal/ml_scorer/calibrator/circuit_breaker the same way full exits already are — right now T1 partials still don't reach those systems, just `closed_trades` directly. Fixed the now-stale `test_pnl_partial_is_net_of_exit_fee` assertions + added 6 tests (`test_entry_fee_reduced_proportionally` ×2, `test_multiple_partials_reduce_entry_fee_correctly`, `test_partial_sell/cover_appends_trade_record` ×2). Full suite: 2913 passed, 0 failures, clean collection. `config.yaml` untouched (`sandbox: true`); no `.env` touched. |
| 2026-06-23 | Claude (scheduled routine) | `fix/macro-data-nan-correlation` → PR #54 | Scheduled "find the single most valuable improvement" pass. Triaged an Explore agent's 3 candidate findings by hand first — all disproven (`src/bot.py`'s `main()` is dead code, `run_all_bots.py` is the real entry point; `funding_scanner.py`'s alleged gap is already caught by an existing broad `try/except`; `dex_arb.py`'s alleged div-by-zero can't occur, the file is an unfinished demo stub and disabled in prod). Real finding from direct reading + a live pandas repro: `src/macro_data.py::_compute_state()`'s 30/60-day Pearson correlation (`Series.corr()`) returns `NaN` on a zero-variance (flat/stale) price window; that NaN flowed straight into `MacroState.btc_gold_corr_30d`/`corr_strength`, and since `nan < x`/`nan >= x` are always `False` in Python, it silently bypassed the "no active macro driver" early-return in `probability_gate._contagion_edge()` (the macro-contagion sizing edge `paper_trading.py` uses) instead of correctly reporting no measurable correlation. Fix: clamp NaN `corr_30`/`corr_60` to `0.0`, routing into the existing safe no-regime path. `src/macro_data.py` had **zero** test coverage before this despite being live-wired into both `paper_trading.py` and `probability_gate.py` — added `tests/test_macro_data.py` (36 tests: CoinGecko fetch/parse, `_compute_state` incl. the NaN regression test, `MacroState` regime properties, cache round-trip, `MacroDataProvider` lifecycle, `alt_beta`). Neutral/shared file (not in either lane row). Full suite: 2944 passed (was 2908), 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched. Also noted but did NOT fix (out of scope, no production caller yet): `KrakenFuturesConnection.get_open_positions()` passes `circuit=self._order_circuit` instead of `self._data_circuit` — same bug class as the merged PR #48 `get_balance()` fix, only exercised by tests today since perps live trading isn't wired through `LiveTrader` yet. |
| 2026-06-24 | Claude (scheduled routine) | `test/altperp-selftest-coverage` → PR #56 | Scheduled "find the single most valuable improvement toward live-trading-readiness" pass. Local `master` again had no common ancestor with `origin/master` (same recurring force-push artifact noted repeatedly above) — reset local to `origin/master`, did not touch the remote. Coverage sweep of `src/altperp/` (the alt-perp directional arm — SHELVED/disabled in `config.yaml` since 2026-06-05, but still wired+tested code, not dead) found **17 modules ship their own deterministic `_selftest()`** (confluence, database, exits, math_utils, mean_reversion, orders, position_sizing, regime, router, signals, telegram, time_utils, trend, backtest, plus the already-wired scenarios/backtest_all) but only 2 were ever called from `tests/test_altperp.py` — the other 15 only ran if someone manually invoked `python -m src.altperp.<module>`, so `pytest` gave zero signal on a regression in any of them. One, `data.py`, is deliberately excluded — its `_selftest()` is a network probe against live Bybit, not a deterministic unit test. Of the 15, 1 (`backtest`) was already verified correct by hand; wired the remaining 14 as thin pytest wrapper functions (`test_<module>_selftest`), exact same pattern as the 2 already in the file — zero production code touched. Highest-value one: `orders.py`'s selftest is the assertion that `PaperExecutor.execute()` raises `NotImplementedError` whenever `config.PAPER_TRADING` is False — the actual safety guarantee `config.yaml`'s "SHELVED, not Kraken-spot executable" comment depends on — that guarantee now runs on every `pytest` invocation instead of only when someone remembers to run the module directly. Pure test addition (`tests/test_altperp.py` +84 lines, 14 new functions, 0 removed). Full suite: 2928 passed (was 2914). `config.yaml`/`.env` untouched (`sandbox: true`, `altperp.enabled: false`). Not in either lane per the map below — `src/altperp/` is its own self-contained package, neither directional-main-loop nor brain/risk file. |
| 2026-06-24 | Claude (scheduled routine) | `fix/live-trading-pnl-pct-fee-basis` → PR | Scheduled "find the single most valuable improvement" pass. Synced first: local was detached at `origin/master` tip (`7dc3ed0`), no divergence this time. Listed open PRs (6: #49 daily-loss halt, #50 Telegram 429 retry, #51 reconcile price fallback, #53 partial-close entry fee, #54 macro NaN correlation, #56 altperp selftest coverage) and confirmed none touch the area found below. An Explore pass surveyed `src/exchange.py` circuit-breaker coverage (clean — no further mismatches after PRs #48/#55/this-lane's prior fix), `arbitrage/` scanners (no correctness bugs), and coverage gaps, then flagged `src/live_trading.py:262`. Verified by hand: `close_long()` computes `pnl` correctly net of `entry_fee`+`exit_fee` (the exact fix PR #16 landed for `pnl` itself), but the very next line computed `pnl_pct` as the raw gross `(exec_price-entry_price)/entry_price*100` — completely ignoring fees, inconsistent with `pnl` on the same `Trade` record and inconsistent with the established pattern already used by `src/paper_trader.py` (`pnl / (cost_basis incl. entry_fee) * 100`, three separate call sites). Concretely: a small fee-heavy live position can show a positive gross price move yet still be a net loser, and `pnl_pct` would report the gain instead of agreeing with `pnl`'s loss — this number flows into logs, the Telegram trade-analysis alert, and the journal/ML-retrain pipeline (`_record_live_trade`). Fix: `cost_basis = pos.size_usd + pos.entry_fee; pnl_pct = pnl / cost_basis * 100` — 2-line change, mirrors `paper_trader.py`'s convention exactly. +1 regression test (`test_pnl_pct_is_net_of_fees_not_gross_price_delta`) constructing exactly this case (heavy entry fee, +2% price move, net loss) and asserting `pnl_pct` agrees in sign with `pnl`. Cross-lane note: touches `src/live_trading.py` (directional/dispatch lane per the map below) — confined to this one PnL-accounting line, no signal/strategy logic touched, consistent with the precedent of prior cross-lane live-trading-readiness fixes (PRs #16-22, #49, #51) on this same file. Not live yet (`DIRECTIONAL_ENABLED=0` default, `config.yaml` still `sandbox: true`); no `.env` touched. Full suite: 2915 passed (was 2914), 0 failures. **Backlog note for the owner:** 6 PRs (#49-#56) are still open/unmerged — worth a merge pass. |
| 2026-06-24 | Claude (scheduled routine) | `fix/bot-main-crash-isolation` → PR | Scheduled "find the single most valuable improvement" pass. Synced first: local `master` had no common ancestor with `origin/master` (same recurring stale-ref issue noted repeatedly above) — reset local to `origin/master`, did not touch the remote. Surveyed the 7 open draft PRs (#49-#51, #53, #54, #56, #57 — all small fee/PnL/retry/coverage fixes in `live_trading.py`/`paper_trader.py`/`notifications.py`/`macro_data.py`/`altperp/`) and confirmed none touch `src/bot.py`, `src/kraken_ws.py`, or `arbitrage/`. Found: `main()`'s `asyncio.gather(bot.start(), run_dashboard(), _run_funding_scanner(...))` (`src/bot.py`) awaited the three top-level subsystems as bare coroutines, only catching `asyncio.CancelledError`. An unhandled exception in ANY ONE of them — including the read-only dashboard, the lowest-stakes of the three — propagates through `gather()` and crashes the whole process; since `crypto-bot.service` is `Restart=always`/`RestartSec=30`, that means a dashboard bug takes live/paper trading and the funding scanner down for 30s+ too, repeating forever if the bug is sticky (a dashboard request bug would loop-crash the entire bot indefinitely). Fix: wrapped all three in the existing, already-tested `supervised()` auto-restart helper (`src/task_supervisor.py`) — the exact same pattern already used for every other subsystem in this file (sentiment/public_ws/book_feed/trade_feed/vol_monitor/advisor/private_ws) but, oddly, never applied to the three most important ones. Confirmed safe: `paper_trading.py` persists trading state via `read_state`/`write_state`, so an in-process `supervised()` restart of `bot.start()` is no worse than today's full-process systemd restart — just faster and without taking the other two subsystems down with it. +4 regression tests (`tests/test_bot_main.py`) — verified they FAIL on pre-fix code (3/4, the exact crash-isolation cases) and pass post-fix. Full suite: 2918 passed (was 2914), 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched. Not in either lane row — `src/bot.py` is the shared top-level entry point, but this is a pure resilience/wiring fix (no strategy/signal logic touched), consistent with how prior cross-lane safety fixes (kill switch, daily-loss halt) have been scoped.
| 2026-06-25 | Claude (scheduled routine) | `fix/funding-scanner-exception-isolation` → PR | Scheduled "find the single most valuable improvement" pass. Synced first: local `master` had again diverged from `origin/master` with no common ancestor (same recurring stale-ref issue noted 2026-06-18/19/20/22) — reset local to `origin/master` (`ccbd2b0`), did not touch the remote. Surveyed the 9 currently-open draft PRs (#49, #50, #51, #53, #54, #56, #57, #59, #60 — live_trading/paper_trader/notifications/macro_data/bot.py/altperp fixes) — none touch `arbitrage/`, so picked the funding scanner instead, which feeds the only arm that actually trades in calm markets (per CLAUDE.md). Coverage sweep showed `arbitrage/funding_scanner.py` at 43% with only `_scan_kraken` tested. Reading `_scan_binance()`/`_scan_bybit()` against the already-correct `_scan_kraken()` pattern found a real gap: `_scan_binance()` had **no per-item try/except** around `float(item.get("lastFundingRate", 0))` at all — a single malformed entry (e.g. a null field from a degraded upstream response) raises, propagates to the outer `except Exception` in `_scan_binance()`, and **silently drops every symbol processed after it** for that scan cycle (premiumIndex returns hundreds of symbols in one response, so this isn't a one-symbol loss). `_scan_bybit()` had a narrower version of the same bug: it caught only `ValueError`, but a null `fundingRate` makes `float(None)` raise `TypeError`, which wasn't caught and had the same cycle-killing effect. Fix: added the missing `try/except (TypeError, ValueError): continue` to `_scan_binance()`, matching `_scan_kraken()`'s established per-item-isolation pattern, and widened `_scan_bybit()`'s guard from `ValueError` to `(TypeError, ValueError)`. No behavior change for well-formed responses — purely a "don't let one bad item nuke the rest of the cycle" fix. Also verified `_scan()`'s majors-preserving top-50 truncation logic (sort by `abs(apy)` desc, keep all `MAJOR_SYMBOLS`, backfill the rest) was already correct — no bug there, just untested. +14 tests in `tests/test_funding_scanner.py`: regression tests for both bugs (valid→malformed→valid item lists, asserting the valid symbols on both sides survive), broader `_scan_binance`/`_scan_bybit` coverage (action labels, non-USDT/zero-rate/below-`MIN_SHOW_APY` filters, non-200 handling) mirroring the existing `_scan_kraken` test style, a `_MultiMockSession` (URL-substring dispatch) to drive `_scan()` end-to-end with synthetic per-exchange data, a majors-preserving-truncation test (2 majors at 15% APY + 60 non-major alts spanning 200%→23% APY confirm both majors survive while only the top-48 alts by APY do), and a fallback-path test (forces the `funding_arb_paper` import to fail via a dummy `sys.modules` entry, restored in `finally`) confirming `_scan()` degrades to a plain top-50 instead of propagating. Coverage on the file: 43%→84%. Full suite: 2928 passed (was 2914), 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched. Neutral file — `arbitrage/funding_scanner.py` isn't listed in either lane row of the lane map (adjacent to but distinct from `arbitrage/funding_arb_paper.py`, which is in the Brain/risk lane); no `funding_arb_paper.py` code touched, only imported read-only for `MAJOR_SYMBOLS`/`_base_symbol` in test data design. **Backlog note for the owner (repeating prior agents' flags):** 9 PRs are open and unmerged (#49–#60, dated 2026-06-22 through 2026-06-24) — worth a merge pass since they keep piling up faster than they land.
| 2026-06-29 | Claude (scheduled routine) | `fix/position-state-nan-sanitization` → PR | Scheduled "find the single most valuable improvement" pass. Surveyed already-open PRs first; none touch position-state serialization. Bug found in `src/paper_trading.py`: `_save_open_positions()` JSON-encoded the position/cash/total_pnl snapshot WITHOUT the `_sanitize()` helper already used ~200 lines earlier in this same file for the dashboard writer. Verified by hand (don't trust the framing on faith): `json.dumps()`'s default `allow_nan=True` does NOT raise on a NaN/Inf field — it silently emits non-standard `NaN`/`Infinity` literals that `json.load()` parses straight back into `float('nan')` with no error. So this is a **silent-corruption risk, not a crash**: a transient NaN in `entry_price`/`cash`/`total_pnl` (e.g. an upstream divide-by-zero) gets written to disk untouched and reloaded as NaN on the next restart, and since NaN comparisons are always `False` in Python, a NaN `entry_price` on a resumed position would make every stop-loss/take-profit check on it silently never fire again, indefinitely across restarts. Fix: (1) wrapped the save payload in `_sanitize()` — mirrors the existing dashboard-writer pattern exactly, NaN/Inf floats become `null`; (2) hardened `_load_open_positions()`'s cash/total_pnl restore in its own try/except, since a sanitized `null` cash now raises `TypeError` on `float(None)` — without isolating it, that exception would propagate to the outer handler and misreport 0 positions restored even though the per-position loop (which already has its own per-item try/except, unchanged) had correctly restored everything else. +6 tests (`TestNaNSanitization` in `tests/test_position_persistence.py`): NaN/Inf written as `null` not a bare `NaN` token, a NaN-corrupted position is skipped while sibling positions still restore, NaN/Inf cash/total_pnl doesn't discard already-restored positions and leaves the account at its fresh default instead of NaN. Full suite: 2920 passed (was 2914), 0 failures. Lane note: `src/paper_trading.py` is dispatch's lane per the map below, but this change is persistence/observability plumbing (state-file save/load), not strategy/signal logic — flagging per coordination rule 3; no signal/gate/sizing code touched. Backlog flag for a future, separately-scoped pass: the same gap (state write without `_sanitize()`) exists in `brain_overseer.py`, `brain_paper.py`, `btc_trend_paper.py`, `conf_paper.py`, `kelly_trend_paper.py`, `lev_perp_paper.py`, `pairs_paper.py`, `regime_arm.py`, `swing_paper.py`, `tsmom_ls_paper.py`, `tsmom_paper.py` — all write state via a bare `json.dumps(state, indent=2)`. `config.yaml` untouched (`sandbox: true`); no `.env` touched.
| 2026-06-29 | Claude (scheduled routine) | `fix/arm-state-nan-sanitization` → PR | Scheduled "find the single most valuable improvement" pass. Synced first (`git fetch` + reset local `master` to `origin/master` — same recurring no-common-ancestor issue noted repeatedly above; local was already clean, nothing lost) and surveyed the 11 open draft PRs (#49–#62) to avoid duplicating — none touch this. PR #62 (same date, merged into this routine's view) had just fixed `src/paper_trading.py::_save_open_positions` for an unsanitized `json.dumps(state)` NaN/Inf bug and explicitly flagged the IDENTICAL gap as an open backlog in 11 standalone arm scripts: `brain_overseer.py`, `brain_paper.py`, `btc_trend_paper.py`, `conf_paper.py`, `kelly_trend_paper.py`, `lev_perp_paper.py`, `pairs_paper.py`, `regime_arm.py`, `swing_paper.py`, `tsmom_ls_paper.py`, `tsmom_paper.py` — every one's `_save_state()` wrote `json.dumps(state, indent=2)` with no NaN/Inf handling. Bug class: `json.dumps`'s default `allow_nan=True` does NOT raise on a NaN/Inf float — it silently emits the non-standard `NaN`/`Infinity` literal, which `json.load` parses straight back into `float('nan')`/`float('inf')` on the next run with no error; since NaN comparisons are always `False` in Python, a NaN that reaches a persisted `entry`/price field can permanently and silently disable that position's stop-loss/take-profit/exit-condition checks, surviving every subsequent restart. Fix: promoted the already-used-once `_clean()` helper in `src/state.py` to a public, documented `sanitize_for_json()` (same recursive NaN/Inf→`None` logic, now explaining the WHY), updated its one internal call site, and routed all 11 scripts' `_save_state()` through it (one new `from src.state import sanitize_for_json` import + wrapping the dumped payload each). No strategy/signal logic touched in any arm. Cross-lane note: `brain_paper.py` + `brain_overseer.py` are in the Brain/risk/observability lane (mine); the other 9 + `src/state.py` are unlisted/neutral shared plumbing — flagging per coordination rule 3. +49 tests (`tests/test_arm_state_sanitization.py`, new file): direct `sanitize_for_json` unit coverage plus, parametrized over all 11 modules, NaN/Inf-written-as-null, round-trip-never-resurrects-NaN, and finite-values-unaffected checks. Regression-proofed by stashing the fix and confirming the new test file fails to even collect (`ImportError: cannot import name 'sanitize_for_json'`) without it, then restoring. Full suite: 2963 passed (was 2914), 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched.
| 2026-06-29 | Claude (scheduled routine) | `test/funding-arb-halt-rollup-coverage` → PR | Scheduled "find the single most valuable improvement" pass. Read `arbitrage/funding_arb_paper.py` (the Kraken/majors funding-arb sim — the only thing that actually trades in calm markets per CLAUDE.md) end-to-end for correctness bugs before touching anything; found none currently reachable — investigated and ruled out two candidates: `net_pnl_since()` including an open position's full lifetime P&L is mathematically equivalent to the 7-day-cutoff window today only because `MAX_HOLD_DAYS=7` exactly matches the one window used (`pnl_7d`), not a live bug; `_deleveraging_regime()`'s `except (TypeError, KeyError)` doesn't catch the `AttributeError` a non-string `symbol` would raise in `_base_symbol()` (reproduced in isolation), but traced into `arbitrage/funding_scanner.py` and confirmed every scan path defaults `symbol` to `""`, never `None` — unreachable today, declined to add speculative validation for it. Pivoted to a coverage sweep (`pytest --cov=arbitrage.funding_arb_paper`, 80%→89%): the 4 biggest gaps were all risk/observability surfaces with zero direct-call tests — `_entry_halt_reason()` (master-kill-switch + per-arm drawdown-cap halt, announce-once-on-engage/resume), `_announce_regime_veto()`'s clear path, `_is_soft_exit()`/`_soft_exit_churn()` (soft-exit churn diagnostics), and `_send_daily_rollup()` (the arm's *only* Telegram observability signal — was 0% covered). +13 tests in `tests/test_funding_arb_paper.py`, pure test addition, no production code changed. Checked the 11 already-open draft PRs (#49-#63) first — none touch this file or its tests, no duplication. Full suite: 2925 passed, 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched. Brain/risk lane (mine) — `arbitrage/funding_arb_paper.py` is listed in this lane. **Backlog note for the owner (repeating prior agents' flags):** 11 PRs are now open and unmerged (#49–#63, dated 2026-06-22 through 2026-06-29) — worth a merge pass since they keep piling up faster than they land.
| 2026-06-29 | Claude (scheduled routine) | `fix/orderflow-ws-reconnect-stale-cvd` → PR | Scheduled "find the single most valuable improvement" pass. Dispatched an Explore survey for untouched high-value gaps (explicitly steered away from the 13 then-open PRs #49–#64, all fee/PnL/NaN/retry/crash-isolation/coverage work). It surfaced 3 candidates; independently re-verified all three before picking one: (1) "DEX/stablecoin arb session leak" (`arbitrage/dex_arb.py`/`stablecoin_arb.py`) — confirmed **zero real impact**, `run_all_bots.py:207-208` hardcodes both `enabled=False` unconditionally, so `start()` never runs regardless of `config.yaml`; declined. (3) "exchange rate-limit window assumption" (`src/exchange.py:165-218`) — re-read the code; the 30s minimum wait is already explicitly documented in the docstring as a deliberate choice, not an implicit bug; declined. (2) **Fixed**: `OrderFlowWS` (`src/orderflow_ws.py`) — the foundation class for the maker-only-microstructure 90-day re-test CLAUDE.md flags as the active next-stage project — never reset its `_cvd_trades`/`_trade_sizes`/`_last_whale` per-symbol state on reconnect. The book channel gets a fresh "snapshot" on every resubscribe (already handled), but the trade channel is pure incremental tape with no replay/snapshot — so a reconnect let pre-disconnect trades silently mix with post-reconnect trades in the CVD-trend window the moment the first new trade arrived, with no error or staleness signal once that first trade landed. Confirmed via repo-wide grep that the `OrderFlowWS` class itself is not yet instantiated anywhere outside its own docstring example (only the pure `obi_from_book` helper is currently used/tested) — i.e. not live today, but exactly the kind of latent correctness bug worth closing **before** the live-loop wiring CLAUDE.md describes as the next stage. Fix: new `_reset_trade_state()` clears CVD/whale/trade-size state on every `_connect()`, called right after the WS connects and before any message is processed. Cross-lane note: `src/orderflow_ws.py` is listed under the "Directional strategy" lane (dispatch) in the map below — flagging per coordination rule 3; change is confined to reconnect-state hygiene, no signal/strategy logic touched. +16 tests (`tests/test_orderflow_ws.py`, new file): CVD/whale accumulation, trend staleness + fail-open semantics, `_reset_trade_state` clears trade state but not book state, and a `_connect()`-level regression test (verified the 6 reconnect/reset tests fail against pre-fix code, pass post-fix). Full suite: 2930 passed (was 2914 on master), 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched. **Backlog note for the owner (repeating prior agents' flags):** 14 PRs are now open and unmerged (#49–#64, dated 2026-06-22 through 2026-06-29) — worth a merge pass since they keep piling up faster than they land.
| 2026-06-30 | Claude (scheduled routine) | `fix/run-all-bots-task-gc-hazard` → PR | Scheduled "find the single most valuable live-trading-readiness improvement" pass. Synced first (`git fetch`, local was a detached HEAD behind `origin/master` — checked out fresh from `origin/master`). Surveyed the now-15 open PRs (#49–66, the calibration/exchange-balance backlog has kept growing since the 2026-06-21/22 notes above) in detail before picking a task — confirmed none touch `run_all_bots.py`. Read `MasterBotRunner.start()` (`run_all_bots.py`, the actual VPS systemd entry point) line by line: every background task it launches — dashboard, DEX arb, stablecoin arb, altperp, and the SIGTERM/SIGINT shutdown handler — calls `asyncio.create_task(...)` and discards the return value. Per the asyncio docs, the event loop holds only a **weak** reference to a `Task`; with no other strong reference, the task can be garbage-collected mid-run with zero error, log line, or Telegram alert (only `scalping_bot`'s task was being kept alive, via the `_critical_tasks` list `_check_critical_tasks()` already monitors). DEX/stablecoin arb and altperp are currently disabled (`main()` forces the first two off; `config.yaml` has `altperp.enabled: false`), so the immediate-impact case is the dashboard task — the one thing in this list that's always on in production, with no health check of its own. Fix: added `MasterBotRunner._spawn()` (`create_task` + append to a new `self._background_tasks` list + done-callback that removes itself so the list doesn't grow unbounded) and routed all five `create_task` call sites (dashboard, dex_arb, stablecoin_arb, altperp, signal-handler shutdown) through it. No crash/restart semantics changed — `_critical_tasks` (scalping_bot only) is untouched, so this is purely the GC-safety fix, not a decision about which tasks should take down the whole service. +5 tests in `tests/test_master_bot_runner.py::TestSpawn`, including a regression test that spawns a task with zero local references, forces `gc.collect()`, and confirms it still completes. Full suite: 2919 passed (was 2908 per the prior installed-deps note; this run also installed `scikit-learn` so the 6 previously-sklearn-gated `test_calibration.py` failures cleared too), 0 failures. `config.yaml` untouched (`sandbox: true`); no `.env` touched. Not in either lane row — `run_all_bots.py` is the shared/neutral process entry point, flagging per coordination rule 3. **Backlog note for the owner (still standing from 2026-06-21):** 15 PRs are now open and unmerged (#49,50,51,53,54,56,57,59,60,61,62,63,64,65,66) — each independently reviewed/tested live-trading-readiness or coverage fix; the pile has grown, not shrunk, since the last note — worth a merge pass.
