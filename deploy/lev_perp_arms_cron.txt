# lev_perp arm family — root crontab lines (VPS). All PAPER. Daily bars close
# at 00:00 UTC; run shortly after so the new bar is final. Staggered to be
# polite to Kraken's public API. Always ./venv/bin/python, never system python.
#
# 2x2 experiment (pre-specified 2026-07-02): exit engine (fixed TP vs ATR trail)
# x risk level (vol-tgt 2%/day cap 3x vs vol-tgt 3%/day cap 5x). The originals
# keep their default env and state files untouched; the aggressive twins are the
# SAME scripts with raised env and their own books. proof_scorecard judges all
# four on the family-wise bar (k grows, bar tightens — that's the point).

# original v1 (control — do not change)
5 0 * * * cd /opt/crypto-bot && ./venv/bin/python lev_perp_paper.py >> logs/lev_perp.log 2>&1

# v2: trailed exits, same risk as v1
7 0 * * * cd /opt/crypto-bot && ./venv/bin/python lev_perp_v2_paper.py >> logs/lev_perp_v2.log 2>&1

# aggressive v1: same code, raised risk, own book
9 0 * * * cd /opt/crypto-bot && LEV_PERP_VOL_TARGET=3.0 LEV_PERP_LEVERAGE=5 LEV_PERP_STATE_FILE=data/lev_perp_agg_state.json ./venv/bin/python lev_perp_paper.py >> logs/lev_perp_agg.log 2>&1

# aggressive v2: trailed exits + raised risk
11 0 * * * cd /opt/crypto-bot && LEV_PERP_VOL_TARGET=3.0 LEV_PERP_LEVERAGE=5 LEV_PERP_V2_STATE_FILE=data/lev_perp_v2_agg_state.json ./venv/bin/python lev_perp_v2_paper.py >> logs/lev_perp_v2_agg.log 2>&1
